[project]
name = "paper-review-harness"
version = "0.4.0"
description = "Codex manuscript review workflow with invariant registries and deterministic acceptance gates"
requires-python = ">=3.11"
dependencies = ["jsonschema>=4.21"]

[tool.pytest.ini_options]
testpaths = ["tests"]
